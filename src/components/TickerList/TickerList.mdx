---
name: TickerList
route: /components/tickerlist
menu: Components
---

import { Playground, Props } from "docz";

import TickerList from "./TickerList";

# TickerList

TODO: desc

## Properties

<Props of={TickerList} />

## Basic usage

<Playground>
  {() => {
    const [favs, setFavs] = React.useState({});
    const [volumeUnit, setVolumeUnit] = React.useState("UST");
    const [showOnlyFavs, setShowOnlyFavs] = React.useState(false);
    return (
      <TickerList
        data={[
          {
            id: "tEOSUST",
            baseCcy: "EOS",
            quoteCcy: "UST",
            changePerc: -0.006,
            lastPrice: 4.019,
            volume: 402,
          },
          {
            id: "tBTCUSD",
            baseCcy: "BTC",
            quoteCcy: "USD",
            changePerc: -0.0006,
            lastPrice: 9000,
            volume: 3940,
          },
          {
            id: "tBTCUST",
            baseCcy: "BTC",
            quoteCcy: "UST",
            changePerc: 0,
            lastPrice: 200,
            volume: 10000,
          },
        ]}
        onRowClick={(pair, data) => alert(`selected pair is ${pair}`)}
        favs={favs}
        saveFavs={setFavs}
        showVolumeUnit
        volumeUnitList={{
          UST: "USDt",
          EOS: "EOS",
          SELF: "SELF",
        }}
        volumeUnit={volumeUnit}
        setVolumeUnit={(val) => setVolumeUnit(val)}
        showOnlyFavs={showOnlyFavs}
        setShowOnlyFavs={setShowOnlyFavs}
      />
    );
  }}
</Playground>
