{
  "name": "ufx-ui-root",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "description": "A UI toolkit to build trading interface",
  "author": "Bitfinex",
  "license": "Apache-2.0",
  "repository": "https://github.com/bitfinexcom/ufx-ui",
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "bootstrap": "npm install && lerna bootstrap",
    "bootstrap:dev": "npm install && lerna bootstrap --force-local"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
